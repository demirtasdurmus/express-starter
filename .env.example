# Example values for environment variables
# Run `cp .env.example .env` and update the values
NODE_ENV=development
HOST=localhost
PORT=8080
CORS_ORIGIN=http://localhost:8080 # Add other origins with comma separated values

# Trust Proxy Configuration
# Number of proxy hops between user and server (e.g., 1 for single nginx, 2 for ALB + CloudFront)
# Use 'true' if you don't know the exact number (less secure but more forgiving)
TRUST_PROXY_HOPS=1